node_modules
.next
out
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Local environment files and credentials
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.staging.local
!.env.example

# Build, test, and platform output
*.tsbuildinfo
coverage
.vercel
test-results/
playwright-report/
artifacts/

# Locally generated document files (never committed, never public)
.storage/

# OpenNext
.open-next

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*

# TAYQAN-2A — the raw Meshy AI source export (~80MB, no web optimization)
# is a local working copy only, never committed. Only the optimized
# public/models/tayqan/tayqan-web.glb (the actual deployable asset, ~3.5MB)
# is tracked.
public/models/tayqan/tayqan.glb
