import { publicFeatures } from "@/lib/config/features";

export type { FeatureStatus, PublicFeature } from "@/lib/config/features";

/** Compatibility export for older public-page imports. */
export const PUBLIC_FEATURES = publicFeatures;
