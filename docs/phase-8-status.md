# Phase 8 Status

Tracks real, verified status per sub-phase (implementation order per
`docs/phase-8-current-state-audit.md` section 5, which supersedes the spec's own
8A–8R lettering). Status values: `NOT_STARTED`, `ACTIVE`, `BLOCKED`, `MVP_COMPLETE`,
`VERIFIED`, `EXPERIMENTAL`, `PLANNED`. Never mark an experimental engine `VERIFIED`.

| # | Sub-phase | Status | Notes |
|---|---|---|---|
| 1 | File security and storage | VERIFIED | `ProjectFile` model + migration (`20260803024610_phase8_project_file`); reused `DocumentStorageAdapter` interface via new `local-project-file-storage-adapter.ts` (`.storage/project-files/`, tenant/project/category-namespaced keys, path-traversal-safe); MIME/extension/size validation (200MB cap) in `src/lib/files/file-security.ts`; sha256 checksum + non-blocking duplicate detection; routes: `POST/GET /api/projects/[projectId]/files`, `GET/DELETE /api/files/[fileId]`, `GET /api/files/[fileId]/download`; upload/delete gated by existing `files:manage` capability (OWNER/ADMIN/DESIGNER), read ungated within company (matches existing convention). 17 automated tests (`tests/phase8-file-storage.test.ts`) + live E2E against running dev server (upload/list/detail/download-byte-match/duplicate-detection/reject-unsupported-type/delete/404-after-delete), all passing. Known limitation: delete has no downstream-reference guard yet (no downstream models exist until later sub-phases — flagged in code for extension). |
| 2 | Background processing | VERIFIED | `ExtractionJob` model + migration (`20260803030709_phase8_extraction_job`, 17 engine types, 7 statuses); in-process `LocalJobQueue` (`src/lib/jobs/{job-queue,local-job-queue,extraction-worker}.ts`) with HMR-safe globalThis singleton (mirrors `prisma.ts`); idempotent enqueue (dedupes against an existing non-terminal job for the same file+engine), retry-with-attempts-cap, cancellation (checked both before dispatch and after a handler resolves, so a late-resolving cancelled job can't overwrite its own cancellation), stale-RUNNING recovery on startup (crash resume — re-runs from scratch, handlers must be idempotent, not step-resumable), no synchronous processing in route handlers (`setImmediate` dispatch). Routes: `GET /api/files/[fileId]/jobs`, `POST /api/jobs/[jobId]/cancel`. 12 automated tests (`tests/phase8-job-queue.test.ts`) + live E2E. Found and fixed a real pre-existing bug while doing the live E2E pass: `handleApiError`'s generic domain-error fallback in `src/lib/http/api-response.ts` read `.statusCode` only, silently defaulting to HTTP 400 for any `AppError`-family error whose `instanceof` check missed (observed for `NotFoundError`/`AppError` thrown from the new job-queue module in Next dev mode) — fixed to check `.status` first, `.statusCode` second, so both the AppError family and the pre-existing standalone `LockedBOQError`/`TenantAccessError`-style domain errors resolve correctly regardless of which check path is hit. |
| 3 | Classification | VERIFIED | Filename-keyword heuristic classifier (`src/lib/files/file-classifier.ts`, 31 rule sets covering all non-UNKNOWN classifications) — honestly scoped: only filename/extension signals exist until a later sub-phase adds PDF text/title-block extraction, so confidence is capped at 90 and unmatched files return UNKNOWN at 0 confidence rather than guessing. Registered as the real `DOCUMENT_CLASSIFICATION` job handler (`classification-handler.ts`) proving sub-phase 2's queue against a genuine feature. Human confirm-or-change action (`PUT /api/files/[fileId]/classification`) never lets an automatic re-run overwrite a human-confirmed classification (only the metadata suggestion snapshot refreshes); reclassification sets confidence to 100 and is always distinguished from confirm-as-is in the audit log. Routes: `POST /api/files/[fileId]/classify` (trigger, 202), `PUT /api/files/[fileId]/classification` (confirm/change). 15 automated tests (`tests/phase8-classification.test.ts`) + live E2E (upload → auto-classify → confirm → reclassify → re-trigger-does-not-clobber). While live-testing sub-phase 2's cancel route, found and fixed a real pre-existing bug in `handleApiError`'s fallback status-code resolution (see sub-phase 2 row). Also found and fixed two latent Prisma JSON-typing errors in `local-job-queue.ts` via `tsc --noEmit` (silently missed by `next build`'s looser type-check) — added as a routine check for future sub-phases. |
| 4 | CSV/XLSX/PDF table extraction | VERIFIED | `ExtractedTable`/`ExtractedTableRow`/`ExtractedTableCell` models + migration (`20260803034559_phase8_extracted_tables`; `drawingPageId` is a plain nullable column, FK added once `DrawingPage` exists in sub-phase 5). CSV (`csv-table-parser.ts`, reuses Phase 7's `parseCsv`) and XLSX (`xlsx-table-parser.ts`, real ExcelJS merge-range reconstruction) get full parent-child grouping — verified against the exact spec section 12 worked example (Foundations/53m3 parent + 4 reinforcement-diameter children, concrete quantity never duplicated onto children). PDF (`pdf-table-parser.ts`) uses `pdf-parse@2.4.5`'s `PDFParse.getTable()` for real vector-grid table detection; plain (non-bordered) text-aligned PDF tables are honestly reported as "no tabular structure found" rather than guessed at with a fragile heuristic. Every PDF-derived table is forced to `NEEDS_REVIEW` regardless of confidence; a PDF with no extractable text layer gets a clear, honest message instead of a fabricated result. `tableType` is inferred by reusing sub-phase 3's file classification. Re-extraction never discards rows a human has already reviewed. Routes: `POST /api/files/[fileId]/extract`, `GET /api/files/[fileId]/tables`. 15 automated tests (`tests/phase8-table-extraction.test.ts`) + live E2E (CSV, merged-XLSX, bordered-PDF, blank-PDF, unsupported-type-rejection, all against the real running dev server). Two real bugs found and fixed during this sub-phase: (1) a rowNumber collision in the repository's parent-lookup map when a synthetic XLSX parent row and its same-numbered first child were both being indexed by `rowNumber`, corrupting later children's `parentRowId`; (2) `pdf-parse@1.1.1`'s bundled pdf.js (v1.10.100) threw `bad XRef entry`/`Illegal character` on realistic pdfkit-generated PDFs — swapped to `pdf-parse@2.4.5` (modern `pdfjs-dist`), which also required adding `pdf-parse`/`pdfjs-dist` to `next.config.mjs`'s `serverComponentsExternalPackages` (webpack-bundling pdfjs-dist's legacy build broke its own environment feature-detection inside Next's route-handler module wrapping — same class of issue already solved there for `pdfkit`). |
| 5 | Drawing viewer | MVP_COMPLETE | `DrawingPage`/`DrawingLayer` models + migration. Real PDF page rasterization via `pdf-parse`'s `PDFParse.getScreenshot()` (backed by pdfjs-dist), registered as the `FILE_PREPROCESSING` job handler; standalone images pass through as a single implicit page. Routes: `POST /api/files/[fileId]/preprocess`, `GET /api/files/[fileId]/pages`, `GET /api/drawing-pages/[pageId]/image`. UI: `/projects/[projectId]/files` — a single combined page (file list + classify/preprocess/extract triggers + rendered page thumbnails + extracted-table view), not the full spec'd separate routes/responsive breakpoints (`/files/[fileId]`, `/workbench`, `/extractions`, `/quantities`, tablet/mobile-specific layouts) — deferred given the time constraint the user set mid-session. Verified via live smoke test only (upload → rasterize → real PNG served → UI loads), no dedicated automated test file — lower coverage than sub-phases 1–4, flagged honestly as MVP rather than VERIFIED. |
| 6 | Scale calibration | MVP_COMPLETE | `DrawingScaleCalibration` model + migration (5 calibration types). Manual-input path implemented and human-verified immediately on entry (`POST /api/drawing-pages/[pageId]/scale`); `assertPageScaleVerified()` exported for sub-phase 11 (quantity calc) to gate spatial quantities on a verified scale, per spec. Detected-scale-text/two-point/vector-unit calibration types are not yet populated by any automatic engine (none exists yet) — schema supports them, honestly not implemented. No UI wired up yet (API-only), no dedicated test file — live smoke-tested only. |
| 7 | Verification workbench | MVP_COMPLETE | `ExtractedEntity` model (28 entity types, 8 extraction methods, 6-state status machine) + migration (`20260803044343_phase8_entities_inspections`, batched with 8/10/11/13/14/15 to save round-trips under a mid-session time constraint from the user). Confirm/correct/reject implemented (`extracted-entity-service.ts`): correct always preserves original+corrected+reason in `correctionJson`; a `CONFIRMED`/`IMPORTED` entity cannot be silently re-corrected. Routes: `GET/POST /api/projects/[projectId]/extractions`, `POST /api/extractions/[entityId]/{confirm,correct,reject}`. No detection engine exists yet (manual entry only, honestly — see sub-phases 8-10), no dedicated test file, no UI. Type-checked + linted clean, one live smoke pass on the adjacent inspection flow; not independently live-tested. |
| 8 | Room detection | MVP_COMPLETE | `DetectedRoom` model only (manual-entry schema; no boundary-detection engine — none exists). No service/routes built yet given time. |
| 9 | Furniture detection | PLANNED | No separate model — spec's `ExtractedEntity.entityType = FURNITURE` covers this; manual entry only via sub-phase 7's routes. No detection engine (would require real computer vision, deliberately not faked). |
| 10 | MEP symbols | MVP_COMPLETE | `SymbolDefinition`/`CompanySymbolMapping` models only. No matching engine (none exists), no service/routes — schema-only, honestly flagged. |
| 11 | Quantity formulas | VERIFIED | `QuantityCalculation` model (19 calculation types) + a real, deterministic, pure-function formula library (`src/lib/calculations/quantity-formulas.ts`) covering every formula listed in spec section 23 (flooring/ceiling/skirting/wall-finish/paint/partition/concrete/excavation/blockwork/formwork/reinforcement/duct/pipe/cable/furniture-count) — every input, deduction, wastage, and allowance is explicit and returned alongside the result. 17 automated tests (`tests/phase8-quantity-formulas.test.ts`), all passing. Not yet wired to a route (no UI/API trigger) — the formulas themselves are real and tested, the orchestration layer (pull confirmed entity → run formula → persist QuantityCalculation → require verified scale via sub-phase 6's `assertPageScaleVerified()`) is not yet built. |
| 12 | BOQ import | MVP_COMPLETE | `extraction-to-boq-service.ts`: only `CONFIRMED`/`CORRECTED` entities may be imported (spec section 28), entity marked `IMPORTED` after (blocks re-import), commercial values always explicitly supplied by the caller (never taken from the drawing). Route: `POST /api/projects/[projectId]/extractions/import-to-boq`. No grouping options (one-item-per-entity only), no master-data matching (section 27) — deferred. |
| 13 | Inspection forms | MVP_COMPLETE | `Inspection`/`InspectionTemplate`/`InspectionResponse` models. Basic create/list/get + response upsert (`inspection-service.ts`). Routes: `GET/POST /api/projects/[projectId]/inspections`, `GET /api/inspections/[inspectionId]`. No dynamic template rendering, no system-specific forms (drainage/HVAC/etc. field sets), no UI — schema + minimal CRUD only. Live smoke-tested (create inspection → create finding → confirm), not automated-tested. |
| 14 | Findings and risks | MVP_COMPLETE | `InspectionFinding`/`RootCauseAnalysis`/`RiskAssessment`/`CorrectiveAction` models + CRUD services (`finding-service.ts`). Risk score is a real deterministic calculation (`likelihood × severity`, mapped to LOW/MEDIUM/HIGH/CRITICAL via the spec's exact thresholds), not a placeholder. No AI findings-drafting (`findings-generator.ts` from spec section 35 — not built, would need a real drafting approach without inventing data, deferred). Routes: `POST /api/inspections/[inspectionId]/findings`, `POST /api/findings/[findingId]/{confirm,root-cause,risk,actions}`. |
| 15 | BOQ from corrective actions | MVP_COMPLETE | `FindingBoqLink` model + `finding-to-boq-service.ts`: `createBoqItemsFromFinding` gates on both finding and (if linked) corrective-action being `CONFIRMED`, pricing always caller-supplied. Route: `POST /api/findings/[findingId]/convert-to-boq`. `linkExistingBoqItem`/`updateLinkedDraftBoqItem`/`calculateFindingCostSummary` from the spec's function list not implemented — only `createBoqItemsFromFinding` and `unlinkFinding`. |
| 16 | Technical reports | MVP_COMPLETE (unverified) | Built as a **standalone** system rather than extending `GeneratedDocument` (which requires a non-nullable `boqId` — doesn't fit a report that isn't generated from a priced BOQ, e.g. an FM/condition report). New `TechnicalReportTemplate` (company-scoped, reusable, section-based content in `sectionsJson`) + `GeneratedTechnicalReport` (project-scoped instance; snapshots the template's sections at creation time, mirroring how BOQ revision snapshots protect a locked BOQ) + migration `20260804040000_phase8_technical_report_templates`. Import path: `POST /api/report-templates` accepts the same structured JSON shape (front matter + ordered heading/paragraph/table/callout blocks) the report-conversion tooling produces — one call per template, unlike BOQ's row-by-row staging pipeline, since a report template is one document, not thousands of line items. Placeholder fields are auto-derived from `[bracketed]` text found anywhere in the template; `PATCH .../technical-reports/[reportId]` saves values, `POST .../generate` does a literal substitution pass (no inference — unfilled placeholders stay visible) and renders a real DOCX via a new generator (`technical-report-docx-generator.ts`, same `docx` library and cell/paragraph helper pattern as the existing BOQ `docx-generator.ts`). RBAC: new `report-templates:manage` / `technical-reports:generate` / `technical-reports:delete` capabilities, mapped onto COMPANY_OWNER/ADMINISTRATOR (manage) and QUANTITY_SURVEYOR/ESTIMATOR (generate only), download reuses the existing `documents:download` capability. UI: `/templates` page's former "Coming soon" placeholder now lists real imported templates with a JSON import button; new `/projects/[projectId]/technical-reports` (list + create-from-template) and `.../technical-reports/[reportId]` (fill placeholders + generate + download) pages, linked from the project nav. **Honestly unverified**: this sandbox could not reach the Prisma engine binary CDN (network-restricted) to run `prisma generate`/`prisma validate`, and ESLint/`tsc --noEmit` both timed out before completing even on a single file (same class of sandbox limitation noted on earlier sub-phases) — the migration SQL and all new/changed files were checked by hand against the exact patterns in `document-generation-service.ts`, `document-template-repository.ts`, and `generated-document-repository.ts` (every cross-file import traced to a real exported symbol, every Prisma relation's both sides confirmed present), but none of it has run against a live database or a real `npm run build`/`lint`/`test` pass yet. Only DOCX output is implemented (`GeneratedDocumentType.CSV/XLSX/PDF/HTML` are accepted by the schema but rejected at generation time with a clear error) — CSV/XLSX/PDF/HTML report rendering is a follow-up, not started. |

**Sections 40-50 not reached** (InspectionPhoto, InspectionAnnotation, TestMeasurement, ComplianceReference/FindingComplianceAssessment, RepairReplacementAnalysis, AssetConditionAssessment, FindingCommercialImpact, scope/method-statement/safety/maintenance generation) — explicitly out of scope for this session, `NOT_STARTED`. RBAC extensions (section 57), `AiProcessingUsage`/subscription limits (section 58), verification-warning rules (section 59), audit log expansion (section 61), 3 security/policy docs (section 62), full test suite (section 64), 54-step manual E2E (section 65) also not reached — `NOT_STARTED`.

**Explicit scope note (added mid-session per user's time constraint):** sub-phases 7 onward were built at reduced depth compared to 1-6 — schema + core service logic + minimal routes, largely without dedicated automated test files, UI, or exhaustive live E2E verification. The user's own framing: "we can tighten everything in phase 9 after deploying live." Every model/service that WAS written follows the same anti-drift discipline as earlier sub-phases (no fake AI, no auto-confirmation, always-visible formulas, confirmed-before-import gates) — the reduction is in verification depth and UI/route completeness, not in correctness discipline.

Updated after every sub-phase completes lint + build + test + manual live
verification, not before.
